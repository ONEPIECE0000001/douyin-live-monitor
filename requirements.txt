# ============================================================
# 抖音直播数据监控与流量预测平台 — Python 依赖清单
# ============================================================
# 安装: pip install -r requirements.txt
# ============================================================

# --- Web 框架 & 服务器 ---
Django>=4.2,<5.0          # Web 框架 (ORM + Admin + REST)
daphne>=4.0.0             # ASGI 服务器 (WebSocket 支撑)

# --- 数据库驱动 ---
mysqlclient>=2.2.0        # MySQL 驱动 (业务统计持久化)
pymongo>=4.6.0            # MongoDB 驱动 (日志流水持久化)
pymysql>=1.1.0            # MySQL 纯 Python 驱动 (check_services 独立脚本)

# --- 消息队列 & 缓存 ---
redis>=5.0.0              # Redis 客户端 (消息队列 + 排行榜缓存)

# --- 浏览器自动化 ---
playwright>=1.42.0        # 无头浏览器 (CDP 寄生模式采集抖音 DOM)

# --- WebSocket (预留) ---
channels>=4.0.0           # Django WebSocket 支持
channels-redis>=4.1.0     # Channels Redis 后端

# --- 自然语言处理 ---
snowNLP>=0.12.0           # 中文情感分析 (弹幕情感打分)

# --- 机器学习 ---
pandas>=2.2.0             # 数据清洗与特征工程
scikit-learn>=1.4.0       # 随机森林回归模型 (流量预测)
joblib>=1.4.0             # 模型序列化/反序列化

# --- 环境变量管理 ---
python-dotenv>=1.0.0      # .env 文件加载 (密码/密钥与代码分离)

